import { db } from "@/lib/db"; import Link from "next/link"; export default async function Page(){const rows=await db.answer.findMany({include:{author:true,question:true},orderBy:{createdAt:"desc"},take:100});return <><h1 className="text-3xl font-bold">回答管理</h1><div className="mt-5 space-y-3">{rows.map(x=><Link className="card block" href={`/questions/${x.questionId}#answer-${x.id}`} key={x.id}><b>{x.question.title}</b><p className="line-clamp-2">{x.content}</p><span className="text-xs">{x.author.username} · {x.status}{x.isOfficial?' · 官方答案':''}</span></Link>)}</div></>}
