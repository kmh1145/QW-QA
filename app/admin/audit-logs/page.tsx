import { db } from "@/lib/db"; export default async function Page(){const rows=await db.auditLog.findMany({include:{operator:{select:{username:true}}},orderBy:{createdAt:"desc"},take:200});return <><h1 className="text-3xl font-bold">操作日志</h1><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr><th>时间</th><th>管理员</th><th>操作</th><th>对象</th><th>IP</th></tr></thead><tbody>{rows.map(x=><tr className="border-t dark:border-slate-800" key={x.id}><td className="py-3">{x.createdAt.toLocaleString("zh-CN")}</td><td>{x.operator.username}</td><td>{x.action}</td><td>{x.targetType}/{x.targetId}</td><td>{x.ipAddress}</td></tr>)}</tbody></table></div></>}
