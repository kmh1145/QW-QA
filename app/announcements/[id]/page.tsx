import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { db } from "@/lib/db"; import { Markdown } from "@/lib/markdown";
export async function generateMetadata({ params }: { params: Promise<{ id: string }> }): Promise<Metadata> { const a = await db.announcement.findUnique({ where: { id: (await params).id } }); return a ? { title: a.title, description: a.summary, openGraph: { title: a.title, description: a.summary, type: "article" } } : {}; }
export default async function AnnouncementPage({ params }: { params: Promise<{ id: string }> }) { const a = await db.announcement.findUnique({ where: { id: (await params).id }, include: { author: true } }); if (!a || !a.isPublic || a.deletedAt) notFound(); return <main className="container-page max-w-3xl py-10"><article className="card"><h1 className="text-3xl font-bold">{a.title}</h1><p className="mt-3 text-sm text-slate-500">发布者：{a.author.username} · {a.publishedAt.toLocaleString("zh-CN")}</p><div className="mt-8"><Markdown>{a.content}</Markdown></div></article></main>; }
