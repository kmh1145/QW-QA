import { db } from "@/lib/db"; import { AuthError, requireApiUser } from "@/lib/auth"; import { assertSameOrigin } from "@/lib/rate-limit"; import { fail, handleError, ok } from "@/lib/api";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { assertSameOrigin(request); const user = await requireApiUser(); const { id } = await params; const old = await db.answerVote.findUnique({ where: { userId_answerId: { userId: user.id, answerId: id } } }); if (old) await db.answerVote.delete({ where: { userId_answerId: { userId: user.id, answerId: id } } }); else await db.answerVote.create({ data: { userId: user.id, answerId: id } }); return ok({ voted: !old }); } catch (e) { if (e instanceof AuthError) return fail(e.message, e.status); return handleError(e); } }
