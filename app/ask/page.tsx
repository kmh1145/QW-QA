import type { Metadata } from "next"; import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db"; import { AskForm } from "@/components/ask-form";
export const metadata: Metadata = { title: "发布问题", robots: { index: false, follow: false } }; export default async function AskPage() { await requireUser(); const categories = await db.category.findMany({ where: { isActive: true }, orderBy: { sortOrder: "asc" } }); return <main className="container-page max-w-3xl py-10"><h1 className="text-3xl font-bold">发布问题</h1><p className="mb-7 mt-2 text-slate-500">请先搜索类似问题，并避免公开手机号、学号、住址等敏感信息。</p><AskForm categories={categories} /></main>; }
