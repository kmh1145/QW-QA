import { notFound } from "next/navigation"; import { db } from "@/lib/db"; import { QuestionCard } from "@/components/question-card";
export default async function CategoryPage({ params }: { params: Promise<{ slug: string }> }) { const category = await db.category.findUnique({ where: { slug: (await params).slug }, include: { questions: { where: { status: "PUBLISHED" }, include: { author: { select: { username: true, role: true, identityBadge: true } }, category: true, _count: { select: { answers: true, votes: true } } }, orderBy: { createdAt: "desc" }, take: 50 } } }); if (!category) notFound(); return <main className="container-page py-10"><h1 className="text-3xl font-bold">{category.name}</h1><p className="mt-2 text-slate-500">{category.description}</p><div className="mt-6 space-y-3">{category.questions.map((q) => <QuestionCard key={q.id} question={q} />)}</div></main>; }
