import type { Metadata } from "next"; import Link from "next/link"; import { db } from "@/lib/db";
export const metadata: Metadata = { title: "问题分类" }; export default async function CategoriesPage() { const categories = await db.category.findMany({ where: { isActive: true }, include: { _count: { select: { questions: true } } }, orderBy: { sortOrder: "asc" } }); return <main className="container-page py-10"><h1 className="text-3xl font-bold">问题分类</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((c) => <Link href={`/categories/${c.slug}`} key={c.id} className="card hover:border-brand-600"><h2 className="font-bold">{c.name}</h2><p className="mt-2 text-sm text-slate-500">{c.description || "查看该分类下的校园问答"}</p><span className="mt-4 block text-sm">{c._count.questions} 个问题</span></Link>)}</div></main>; }
