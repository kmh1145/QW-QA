import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db"; import { MeNav } from "@/components/me-nav"; import Link from "next/link"; export default async function Page(){const u=await requireUser();const rows=await db.answer.findMany({where:{authorId:u.id,deletedAt:null},include:{question:true},orderBy:{updatedAt:"desc"}});return <main className="container-page py-10"><MeNav/><h1 className="text-3xl font-bold">我的回答</h1><div className="mt-5 space-y-3">{rows.map(x=><Link className="card block" href={`/questions/${x.questionId}#answer-${x.id}`} key={x.id}><b>{x.question.title}</b><p className="mt-2 line-clamp-2 text-sm">{x.content}</p></Link>)}</div></main>}
