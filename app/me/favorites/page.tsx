import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db"; import { MeNav } from "@/components/me-nav"; import Link from "next/link"; export default async function Page(){const u=await requireUser();const rows=await db.favorite.findMany({where:{userId:u.id},include:{question:true},orderBy:{createdAt:"desc"}});return <main className="container-page py-10"><MeNav/><h1 className="text-3xl font-bold">我的收藏</h1><div className="mt-5 space-y-3">{rows.map(x=><Link className="card block" href={`/questions/${x.questionId}`} key={x.questionId}>{x.question.title}</Link>)}</div></main>}
