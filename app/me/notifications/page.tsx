import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db"; import { MeNav } from "@/components/me-nav"; import Link from "next/link"; import { ActionButton } from "@/components/action-button"; export default async function Page(){const u=await requireUser({verified:false});const rows=await db.notification.findMany({where:{userId:u.id,deletedAt:null},orderBy:{createdAt:"desc"},take:100});return <main className="container-page py-10"><MeNav/><div className="flex justify-between"><h1 className="text-3xl font-bold">消息通知</h1><ActionButton endpoint="/api/notifications/read-all" label="全部标为已读"/></div><div className="mt-5 space-y-3">{rows.map(x=><div className={`card ${!x.readAt?"border-brand-600":""}`} key={x.id}>{x.link?<Link href={x.link}><b>{x.title}</b></Link>:<b>{x.title}</b>}<p className="mt-1 text-sm text-slate-500">{x.content}</p></div>)}</div></main>}
