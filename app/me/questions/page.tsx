import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db"; import { MeNav } from "@/components/me-nav"; import Link from "next/link"; export default async function Page(){const u=await requireUser();const rows=await db.question.findMany({where:{authorId:u.id,deletedAt:null},orderBy:{updatedAt:"desc"}});return <main className="container-page py-10"><MeNav/><h1 className="text-3xl font-bold">我的提问</h1><div className="mt-5 space-y-3">{rows.map(x=><Link className="card block" href={`/questions/${x.id}`} key={x.id}>{x.title}<span className="ml-2 text-xs text-slate-500">{x.status}</span></Link>)}</div></main>}
