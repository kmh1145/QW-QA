import Link from "next/link"; import { db } from "@/lib/db"; import { QuestionCard } from "@/components/question-card";
export const dynamic = "force-dynamic";
export default async function Home() { const [questions, announcements, categories, faqs] = await Promise.all([db.question.findMany({ where: { status: "PUBLISHED" }, include: { author: { select: { username: true, role: true, identityBadge: true } }, category: true, _count: { select: { answers: true, votes: true } } }, orderBy: [{ isPinned: "desc" }, { createdAt: "desc" }], take: 6 }), db.announcement.findMany({ where: { isPublic: true }, orderBy: [{ isPinned: "desc" }, { publishedAt: "desc" }], take: 4 }), db.category.findMany({ where: { isActive: true }, orderBy: { sortOrder: "asc" }, take: 10 }), db.knowledgeDocument.findMany({ where: { isEnabled: true, status: "READY", category: "FAQ" }, take: 5 })]); return <main><section className="bg-gradient-to-b from-brand-50 to-slate-50 py-16 text-center dark:from-brand-950/40 dark:to-slate-950"><div className="container-page"><h1 className="text-3xl font-bold sm:text-5xl">清华附中湾区学校新生Q&A</h1><p className="mt-5 text-xl font-medium">有问题，先搜索；找不到，再提问。</p><p className="mx-auto mt-2 max-w-2xl text-slate-600 dark:text-slate-300">汇集校园经验、官方资料和 AI 检索能力，帮助新生快速找到可靠答案。</p><form action="/search" className="mx-auto mt-8 flex max-w-2xl gap-2"><label className="sr-only" htmlFor="home-search">搜索校园问题</label><input id="home-search" className="input !min-h-14" name="q" placeholder="搜索报到、宿舍、课程、社团……" /><button className="btn !min-h-14">搜索</button></form><div className="mt-5 flex justify-center gap-3"><Link href="/ask" className="btn-secondary">发布问题</Link><Link href="/ai" className="btn">向 AI 助手提问</Link></div><p className="mt-3 text-xs text-slate-500">AI 回答可能存在遗漏，重要事项请以学校最新通知为准。</p></div></section><div className="container-page grid gap-8 py-10 lg:grid-cols-[2fr_1fr]"><div><h2 className="mb-4 text-2xl font-bold">最新与热门问题</h2><div className="space-y-3">{questions.length ? questions.map((q) => <QuestionCard key={q.id} question={q} />) : <div className="card text-slate-500">还没有问题，欢迎发布第一个问题。</div>}</div><Link href="/questions" className="btn-secondary mt-4">查看全部问题</Link></div><aside className="space-y-6"><section className="card"><h2 className="text-lg font-bold">官方公告</h2><ul className="mt-3 space-y-3">{announcements.map((a) => <li key={a.id}><Link href={`/announcements/${a.id}`} className="hover:text-brand-600">{a.isPinned && "【置顶】"}{a.title}</Link></li>)}{!announcements.length && <li className="text-slate-500">暂无公告</li>}</ul></section><section className="card"><h2 className="text-lg font-bold">常见分类</h2><div className="mt-3 flex flex-wrap gap-2">{categories.map((c) => <Link key={c.id} href={`/categories/${c.slug}`} className="rounded-lg bg-brand-50 px-3 py-2 text-sm text-brand-800 transition hover:bg-brand-100 dark:bg-brand-950/50 dark:text-brand-200 dark:hover:bg-brand-900">{c.name}</Link>)}</div></section><section className="card"><h2 className="text-lg font-bold">常见 FAQ</h2><ul className="mt-3 list-disc space-y-2 pl-5">{faqs.map((f) => <li key={f.id}><Link href={`/knowledge/${f.id}`} className="rounded-sm text-brand-700 underline-offset-4 transition hover:text-brand-900 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-500 dark:text-brand-300 dark:hover:text-brand-100">{f.title}</Link></li>)}</ul></section><section className="card"><h2 className="font-bold">新用户使用引导</h2><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm"><li>先用关键词搜索已有答案</li><li>确认资料来源和更新时间</li><li>找不到时清楚地描述问题</li><li>保护自己和他人的个人信息</li></ol></section></aside></div></main>; }
