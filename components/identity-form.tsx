"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import type { IdentityBadge } from "@prisma/client";
export function IdentityForm({ current }: { current: IdentityBadge }) { const router = useRouter(); const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const identityBadge = new FormData(e.currentTarget).get("identityBadge"); const res = await fetch("/api/me/identity", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ identityBadge }) }); const json = await res.json(); setMessage(json.data ? "身份已更新" : json.error?.message); if (res.ok) router.refresh(); } return <form onSubmit={submit} className="card"><label><span className="label">学生/毕业生身份</span><select name="identityBadge" defaultValue={current === "TEACHER" ? "NONE" : current} className="input" disabled={current === "TEACHER"}><option value="NONE">暂无标识</option><option value="GRADE_1">高一</option><option value="GRADE_2">高二</option><option value="GRADE_3">高三</option><option value="ALUMNI">已毕业学长学姐</option></select></label>{current === "TEACHER" ? <p className="mt-3 text-sm">学校教师标识由管理员授予，不能自行修改。</p> : <><p className="mt-3 text-xs text-slate-500">该身份由用户自行选择，不代表学校官方认证。修改可能受学年冷却时间限制。</p><button className="btn mt-4">保存身份</button></>}{message && <p className="mt-3 text-sm">{message}</p>}</form>; }
