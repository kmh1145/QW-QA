"use client";
import { useState } from "react";
export function ResendForm() { const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const email = new FormData(e.currentTarget).get("email"); const res = await fetch("/api/auth/resend-verification", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); const json = await res.json(); setMessage(json.data?.message ?? json.error?.message ?? "请求失败"); } return <form onSubmit={submit} className="card mt-6"><label><span className="label">注册邮箱</span><input className="input" type="email" name="email" required /></label><button className="btn mt-4 w-full">重新发送验证邮件</button>{message && <p className="mt-3 text-sm">{message}</p>}</form>; }
